<!DOCTYPE html>
<html lang="en" data-theme="night">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Update Pemilik</title>
    <link
      href="https://cdn.jsdelivr.net/npm/daisyui@4.7.2/dist/full.min.css"
      rel="stylesheet"
      type="text/css"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <%- include("../partials/_nav") %>
    <div class="w-full py-10 flex flex-col gap-7 items-center">
      <p class="text-3xl font-bold uppercase">Update Pemilik</p>
      <form
        action="/pemilik/update/<%= data.id_pemilik %>"
        method="POST"
        class="w-1/3 flex flex-col gap-5"
      >
        <div class="w-full flex flex-col gap-2">
          <label for="nama_pemilik" class="font-semibold capitalize">
            Nama Pemilik
          </label>
          <input
            autocomplete="off"
            id="nama_pemilik"
            name="nama_pemilik"
            type="text"
            placeholder="Type here"
            class="input input-bordered w-full"
            value="<%= data.nama_pemilik %>"
          />
        </div>
        <div class="w-full flex flex-col gap-2">
          <label for="alamat" class="font-semibold capitalize"> Alamat </label>
          <input
            autocomplete="off"
            id="alamat"
            name="alamat"
            type="text"
            placeholder="Type here"
            class="input input-bordered w-full"
            value="<%= data.alamat %>"
          />
        </div>
        <div class="w-full flex flex-col gap-2">
          <label for="nomer_hp" class="font-semibold capitalize">
            Nomer Hp
          </label>
          <input
            autocomplete="off"
            id="nomer_hp"
            name="nomer_hp"
            type="text"
            placeholder="Type here"
            class="input input-bordered w-full"
            value="<%= data.nomer_hp %>"
          />
        </div>
        <div class="w-full">
          <button
            type="submit"
            class="btn btn-md btn-active btn-primary w-full text-white"
          >
            Update
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
