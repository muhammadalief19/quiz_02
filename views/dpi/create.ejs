<!DOCTYPE html>
<html lang="en" data-theme="night">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Create DPI</title>
    <link
      href="https://cdn.jsdelivr.net/npm/daisyui@4.7.2/dist/full.min.css"
      rel="stylesheet"
      type="text/css"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <%- include("../partials/_nav") %>
    <div class="w-full py-10 flex flex-col gap-7 items-center">
      <p class="text-3xl font-bold uppercase">Create DPI</p>
      <form action="/dpi/store" method="POST" class="w-1/3 flex flex-col gap-5">
        <div class="w-full flex flex-col gap-2">
          <label for="nama_dpi" class="font-semibold capitalize">
            Nama DPI
          </label>
          <input
            autocomplete="off"
            id="nama_dpi"
            name="nama_dpi"
            type="text"
            placeholder="Type here"
            class="input input-bordered w-full"
          />
        </div>
        <div class="w-full flex flex-col gap-2">
          <label for="luas_dpi" class="font-semibold capitalize">
            Luas DPI
          </label>
          <input
            autocomplete="off"
            id="luas_dpi"
            name="luas_dpi"
            type="text"
            placeholder="Type here"
            class="input input-bordered w-full"
          />
        </div>
        <div class="w-full">
          <button
            type="submit"
            class="btn btn-md btn-active btn-primary w-full text-white"
          >
            Create
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
